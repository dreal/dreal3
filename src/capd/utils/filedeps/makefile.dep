# This file has been generated automatically by File Dependencies Tracer.
# Do not edit this file! It will be overwritten without warning.

# ===========================================================================
# Rules for MAKE on how to build object files (with suitable paths).
# ===========================================================================

${OBJ}filedeps.o: ${SRC}filedeps.cpp arg.h config.h hashsets.h integer.h \
	multitab.h textfile.h timeused.h words.h
	${CCI} -o ${OBJ}filedeps.o -c ${SRC}filedeps.cpp

${OBJ}arg.o: ${SRCLIB}arg.cpp arg.h config.h
	${CCI} -o ${OBJ}arg.o -c ${SRCLIB}arg.cpp

${OBJ}integer.o: ${SRCLIB}integer.cpp config.h integer.h textfile.h
	${CCI} -o ${OBJ}integer.o -c ${SRCLIB}integer.cpp

${OBJ}textfile.o: ${SRCLIB}textfile.cpp config.h textfile.h
	${CCI} -o ${OBJ}textfile.o -c ${SRCLIB}textfile.cpp

${OBJ}timeused.o: ${SRCLIB}timeused.cpp config.h timeused.h
	${CCI} -o ${OBJ}timeused.o -c ${SRCLIB}timeused.cpp


# ===========================================================================
# Rules for MAKE on how to build executables with paths and extensions.
# ===========================================================================

${BIN}filedeps${EXE}: ${OBJ}filedeps.o ${OBJ}arg.o ${OBJ}integer.o \
	${OBJ}textfile.o ${OBJ}timeused.o
	${CC} -o ${BIN}filedeps${EXE} \
	${OBJ}filedeps.o ${OBJ}arg.o ${OBJ}integer.o ${OBJ}textfile.o \
	${OBJ}timeused.o ${CCLIB}

# The end.


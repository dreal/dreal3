# Settings for a typical GNU C++ compiler in Linux/Unix.
# The standard X Window library is used for graphics (if any).
# It compiles Multiprecision Arithmetics built on GMP and MPFR
# so both packages have to be installed.

SYSLIB  = -lpthread -lmpfr -lgmp
SYSLIBG = -L/usr/X11R6/lib/ -lX11 -lpthread -lmpfr -lgmp
SYSINCL =
COMPILE = g++ -D__HAVE_MPFR__ -O2 -ansi -pedantic -Wall -frounding-math
LINK    = g++ -s
LINKGUI = g++ -s
MAKELIB = ar cru
IDXLIB  = ranlib
REMOVE  = rm -f
MKDIR   = mkdir
EXE     = 
LIBEXT  = .a
OBJEXT  = .o
INSTALLDIR = /usr/local/

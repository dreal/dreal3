#include <string>
#include "new_realpaver/variable.h"
#include "new_realpaver/function.h"

using std::string;

namespace realpaver {
// bool function::insertVar(string const & s) {
//     auto const it = find_if(_lvars.begin(), _lvars.end(),
//                             [&s](variable const & v) {
//                                 return v._name == s;
//                             });
//     if (it == _lvars.end()) {
//         _lvars.emplace_back(s);
//         return true;
//     }
//     return false;
// }
}

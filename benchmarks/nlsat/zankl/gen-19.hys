-- translated from ./zankl/gen-19.smt2
DECL
    -- the variables
    float [-1000, 1000] x0, x1;
EXPR
    -- the constraints to be solved
    ((2 * x0) = 1);
    ((x1 * x1) = 2);
    !(((-1 * x0) + x1) >= 0);

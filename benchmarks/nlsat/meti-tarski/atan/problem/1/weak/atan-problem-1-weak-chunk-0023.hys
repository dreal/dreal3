-- translated from ./meti-tarski/atan/problem/1/weak/atan-problem-1-weak-chunk-0023.smt2
DECL
    -- the variables
    float [-1000, 1000] x0, x1, x2;
EXPR
    -- the constraints to be solved
    (!(((3 * x1) + x2) <= 0) and !(x0 <= 0));

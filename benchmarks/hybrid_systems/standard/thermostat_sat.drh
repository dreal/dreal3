[10, 50.0] x;
[0.2] a;
[0, 10] time;

%%
{ mode 1;
  invt:
	(>= x 19);

  flow:
        d/dt[x] = (- 0 (* a x));
  jump:
        (<= x 19) ==> @2 (= x' x);
}

{ mode 2;

  invt:
	(<= x 21);
  flow:
        d/dt[x] = (- 0 (* a (- x 30)));
  jump:
        (>= x 21) ==> @1 (= x' x);
}

%%
init:
@1	(= x 20);

goal:
@1	(= x 20.5);

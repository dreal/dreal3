//watertank model, Page 50 of Hybrid Systems: Modeling, Analysis, and Control 2008

[0, 100.0] x;

[0, 100] time;

%%
{ mode 1;

  flow:
        d/dt[x] = (- (* a x));
  jump:
	(<= x 19) ==> @2 (= x' x);
}

{ mode 2;

  flow:
        d/dt[x] = (- (* a (- x 30)));
  jump:
	(>= x 21) ==> @1 (= x' x);
}

%%
init:
@1	( and (> x1 r1) (> x2 r2) );

goal:
@1	(= x 40);

[0, 20] x;
[9.8] g;
[-1000, 1000] v;
[0, 100] time;    // Default time interval for each mode
%%
{ mode 1;
  invt:
	(>= x 0);
  flow:
	d/dt[x] = v;
	d/dt[v] = g;
  jump:
	(= x 0) ==> @2 (and (= x' x) (= v' (- 0 v)));

}
{
  mode 2;
  invt:
	(>= x 0);
  flow:
	d/dt[x] = v;
	d/dt[v] = (- 0 g);
  jump:
	(= v 0) ==> @1 true;
}
%%
init:
@1	(and (= x 10) (= v 0));

goal:
@2	(= x 9);


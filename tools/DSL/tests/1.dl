// this is comment
#define g 9.8

process flow1 (real x) {
    d/dt(x) = 1;
}

process flow2 (real x) {
    d/dt(x) = -x;
}

int main () {
    real x = 1;
    int mode = 1;

    proceed[0 : 100] {
        switch (mode) {
            case 1:
                flow1(x);
            case 2:
                flow2(x);
        }
    }
}